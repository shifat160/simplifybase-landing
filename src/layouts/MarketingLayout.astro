---
import BaseLayout from './BaseLayout.astro';
import SiteNav from '~/components/nav/SiteNav.astro';
import SiteFooter from '~/components/nav/SiteFooter.astro';

/** Every non-docs page: home, product pages, blog, about, legal. */
interface Props {
  title: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
  brandSuffix?: boolean;
  accent?: string;
  noindex?: boolean;
  /* Forwarded to BaseLayout by the spread below; declared so a caller passing
     them type-checks. Only blog posts use them today. */
  publishedTime?: Date;
  modifiedTime?: Date;
  author?: string;
  tags?: readonly string[];
}

const props = Astro.props;
---

<BaseLayout {...props}>
  <slot name="head" slot="head" />
  <SiteNav />
  <main id="main">
    <slot />
  </main>
  <SiteFooter />
</BaseLayout>
